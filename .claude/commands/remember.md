---
description: Save something to project memory (user/preferences/decisions/people)
---

The user wants to save the following to project memory:

$ARGUMENTS

## Your task

1. Decide which of the four files in `C:/Users/Eran/Desktop/Personal/JobSearch/memory/` fits best:
   - `user.md` — persistent facts about the user (role, background, goals, expertise)
   - `preferences.md` — how to work (tone, tools, workflows, dos and donts)
   - `decisions.md` — architectural/process decisions. Each entry MUST have a `**Why:**` and `**How to apply:**` line
   - `people.md` — humans/companies relevant to the project, with their role

2. **Read the target file first.** Check whether this information already exists in some form. If it does, **edit the existing entry** — never create a duplicate. Only append a new entry if nothing related exists.

3. Match the existing format in the file (headers, structure, tone). For decisions, timestamp with today's date as a `## YYYY-MM-DD —` heading.

4. Keep it terse. One fact per entry. If the user gave you multiple items, split them across the right files.

5. Show a one-line confirmation of what was saved and where. Do not summarize the conversation or restate what was saved in full — the diff is visible.
